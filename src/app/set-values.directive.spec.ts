import { SetValuesDirective } from './set-values.directive';

describe('SetValuesDirective', () => {
  it('should create an instance', () => {
    const directive = new SetValuesDirective();
    expect(directive).toBeTruthy();
  });
});
